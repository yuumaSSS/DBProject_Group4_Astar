import 'package:flutter/material.dart';

class ManageAddScreen extends StatelessWidget {
  const ManageAddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}