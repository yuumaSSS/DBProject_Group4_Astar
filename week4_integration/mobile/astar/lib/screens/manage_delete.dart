import 'package:flutter/material.dart';

class ManageDeleteScreen extends StatelessWidget {
  const ManageDeleteScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}