import 'package:flutter/material.dart';

class ManageUpdateScreen extends StatelessWidget {
  const ManageUpdateScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}